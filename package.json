{
  "name": "sprucemysite",
  "type": "module",
  "version": "1.0.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node tools/generate-sitemap.mjs && node tools/generate-llms-full.mjs",
    "preview": "astro preview",
    "check": "astro check && npm run audit",
    "audit": "npm audit --audit-level=high"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0",
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^7.0.2"
  }
}
